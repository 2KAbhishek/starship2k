format = """
[](fg:#1688f0)\
$directory\
[]( bg:#3ED50A)\
$git_branch\
$git_status\
[](fg:#3ED50A)\
$fill\
$c\
$elixir\
$elm\
$golang\
$haskell\
$java\
$julia\
[](fg:#6BA15C)\
$nodejs\
$nim\
$rust\
$scala\
$docker_context\
[](fg:#33658a bg:#6BA15C)\
$time\
[](fg:#33658A)\
$line_break $character
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like  to save some space
[directory]
style = "bg:#1688f0"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = " Documents"
"Downloads" = "  Downloads"
"Music" = "  Music"
"Pictures" = "  Pictures"
"Projects" = "  Projects"
# Keep in mind that the order matters. For example:
# "Important Documents" = "  "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important  " = "  "
[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "fg:black bg:cyan"
format = '[ $time ]($style)'

[c]
symbol = " "
style = "fg:black bg:#3F4347"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "fg:black bg:#06969A"
format = '[ $symbol $context ]($style) $path'

[elixir]
symbol = " "
style = "fg:black bg:#422758"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "fg:black bg:#F0AD00"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = "  "
style = "fg:black bg:green"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "fg:black bg:green"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "fg:black bg:#00ACD7"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "fg:black bg:#9C4B89"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "fg:black bg:#F0931C"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "fg:black bg:#CA3C32"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "fg:black bg:#6BA15C"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = " "
style = "fg:black bg:#F7E852"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "fg:black bg:#C26F2E"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "fg:black bg:#D73222"
format = '[ $symbol ($version) ]($style)'
